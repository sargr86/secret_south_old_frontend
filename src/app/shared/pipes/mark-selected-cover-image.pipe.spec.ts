import { MarkSelectedCoverImagePipe } from './mark-selected-cover-image.pipe';

describe('MarkSelectedCoverImagePipe', () => {
  it('create an instance', () => {
    const pipe = new MarkSelectedCoverImagePipe();
    expect(pipe).toBeTruthy();
  });
});

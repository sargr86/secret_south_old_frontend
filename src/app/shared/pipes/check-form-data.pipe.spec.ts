import { CheckFormDataPipe } from './check-form-data.pipe';

describe('CheckFormDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckFormDataPipe();
    expect(pipe).toBeTruthy();
  });
});

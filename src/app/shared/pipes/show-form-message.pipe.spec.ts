import { ShowFormMessagePipe } from './show-form-message.pipe';

describe('ShowFormMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowFormMessagePipe();
    expect(pipe).toBeTruthy();
  });
});

import { GetTourFormFieldsPipe } from './get-tour-form-fields.pipe';

describe('GetTourFormFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTourFormFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});

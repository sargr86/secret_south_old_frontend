import { BuildFormDataPipe } from './build-form-data.pipe';

describe('BuildFormDataPipe', () => {
  it('create an instance', () => {
    const pipe = new BuildFormDataPipe();
    expect(pipe).toBeTruthy();
  });
});

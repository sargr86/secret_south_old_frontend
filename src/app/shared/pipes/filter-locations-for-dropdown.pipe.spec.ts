import { FilterLocationsForDropdownPipe } from './filter-locations-for-dropdown.pipe';

describe('FilterLocationsForDropdownPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLocationsForDropdownPipe();
    expect(pipe).toBeTruthy();
  });
});

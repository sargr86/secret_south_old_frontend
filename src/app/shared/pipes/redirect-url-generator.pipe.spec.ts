import { RedirectUrlGeneratorPipe } from './redirect-url-generator.pipe';

describe('RedirectUrlGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new RedirectUrlGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});

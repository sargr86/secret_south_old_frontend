import { FilterEndCheckoutDatesPipe } from './filter-end-checkout-dates.pipe';

describe('FilterEndCheckoutDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEndCheckoutDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
